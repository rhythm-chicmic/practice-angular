import { DisplayDataDirective } from './display-data.directive';

describe('DisplayDataDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayDataDirective();
    expect(directive).toBeTruthy();
  });
});
